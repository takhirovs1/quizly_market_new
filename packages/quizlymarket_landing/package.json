{
  "name": "quizlymarket-landing",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "QuizlyMarket — responsive landing (Vite + vanilla JS, Clean Architecture)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
